export const APP_NAME = process.env.NEXT_PUBLIC_APP_NAME?.trim() || 'Torq Flows';

export const APP_DESCRIPTION =
  process.env.NEXT_PUBLIC_APP_DESCRIPTION?.trim() ||
  'Design, run, and monitor visual workflow automations with a durable execution runtime.';
